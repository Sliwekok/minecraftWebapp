$('#create_new_server_form_step_1_name').on('change', function (){
    // replace whitespaces with underscore and quotes to empty
    // let worldSave = $(this).val();
    // worldSave = worldSave.replace(/ /g,"_");
    // worldSave = worldSave.replace(/["']/g, "");
    // $(this).val(worldSave);
});
